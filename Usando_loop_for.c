#include <stdio.h>
int main(){

// inicialização, condição, incremento
/* Com o for, vc nao precisa informar uma variável no inicio do codigo
como de costume, basta criar dentro do próprio código for*/

for (int i = 1; i >= 5; i--)
{
    printf("%d\n", i);
}
return 0;

}
