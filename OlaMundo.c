#include <stdio.h>
int main() {
    int a = 10;
    int b = 3;
    float quociente = (double) a / b;//O valor inteiro foi transformado em um float ou double


    printf("A soma de A+B é: %.2f\n", quociente);
    //printf("A diferença de A-B é: %.2f\n", diferenca);
    //printf("O produto de A*B é: %.2f\n", produto);
    //printf("O quociente de A/B é: %.2f\n", quociente);

        return 0;

}
